@model IList<AcademicDocumentRagSystem.RazorPages.ViewModels.TeacherCourseCardViewModel>
<div class="row row-cards g-3">
    @foreach (var card in Model)
    {
        <div class="col-md-6 col-lg-4">
            <div class="card edurag-card file-card h-100">
                <div class="card-body d-flex flex-column">
                    <div class="d-flex align-items-center gap-3 mb-3">
                        <span class="stat-icon-box bg-gradient-warm text-white">
                            @{ ViewData["Icon"] = "book"; ViewData["IconSize"] = "20"; }
                            @await Html.PartialAsync("_Icons")
                        </span>
                        <div class="min-width-0">
                            <div class="small fw-semibold text-muted-edurag">@card.Course.Code</div>
                            <h3 class="font-display fw-bold fs-5 mb-0 text-truncate">@card.Course.Name</h3>
                        </div>
                    </div>
                    <div class="row text-center g-0 border rounded-3 py-2 mb-3" style="border-color:var(--edurag-border)!important;background:var(--edurag-sidebar);">
                        <div class="col-4 border-end" style="border-color:var(--edurag-border)!important;">
                            <div class="font-display fw-bold">@card.DocumentCount</div>
                            <div class="text-muted-edurag" style="font-size:11px;">Tài liệu</div>
                        </div>
                        <div class="col-4 border-end" style="border-color:var(--edurag-border)!important;">
                            <div class="font-display fw-bold">@card.IndexedCount</div>
                            <div class="text-muted-edurag" style="font-size:11px;">Indexed</div>
                        </div>
                        <div class="col-4">
                            <div class="font-display fw-bold">@card.TotalChunks.ToString("N0")</div>
                            <div class="text-muted-edurag" style="font-size:11px;">Chunks</div>
                        </div>
                    </div>
                    <div class="d-flex gap-2 mt-auto">
                        <a asp-page="/Teacher/Library" asp-route-course="@card.Course.Code" class="btn btn-sm btn-edurag-outline flex-grow-1">Tài liệu</a>
                        <a asp-page="/Teacher/Upload" class="btn btn-sm btn-edurag-warm flex-grow-1">Upload</a>
                    </div>
                </div>
            </div>
        </div>
    }
    @if (!Model.Any())
    {
        <div class="col-12">
            <div class="card edurag-card">
                <div class="card-body text-center text-muted-edurag py-5">Chưa được gán môn học. Liên hệ Admin.</div>
            </div>
        </div>
    }
</div>
